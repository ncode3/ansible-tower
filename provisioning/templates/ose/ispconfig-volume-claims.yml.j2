apiVersion: "v1"
kind: "PersistentVolumeClaim"
metadata:
  name: "ispconfig-REPLACEME"
spec:
  accessModes:
    - "ReadWriteOnce"
  resources:
    requests:
      storage: "2Gi"
  volumeName: "ispconfig"
